# Builder.io — visual page builder (kéo-thả). Lấy "Public API Key" trong
# Builder.io → Account Settings → API Keys, rồi dán vào .env.local:
#   NEXT_PUBLIC_BUILDER_API_KEY=xxxxxxxxxxxx
NEXT_PUBLIC_BUILDER_API_KEY=
