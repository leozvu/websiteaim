# Normalize line endings — store as LF in the repo, check out native on each OS.
* text=auto eol=lf

# Binary / asset files — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
